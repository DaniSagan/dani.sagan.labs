import { Component } from '@angular/core'; import { FractalExplorerComponent, FractalRenderer, paint } from '../fractal-explorer/fractal-explorer.component';
@Component({selector:'app-cantor-dust',standalone:true,imports:[FractalExplorerComponent],templateUrl:'./cantor-dust.component.html',styleUrl:'./cantor-dust.component.css'}) export class CantorDustComponent { static title='Polvo de Cantor'; static route='cantor-dust'; readonly draw:FractalRenderer=(ctx,iterations)=>{paint(ctx,50);const rec=(x:number,y:number,s:number,n:number):void=>{if(!n){ctx.fillRect(x,y,s,s);return;}const t=s/3;for(const a of [0,2])for(const b of [0,2])rec(x+a*t,y+b*t,t,n-1);};rec(75,75,530,iterations);}; }
