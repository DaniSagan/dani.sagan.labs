import { Component } from '@angular/core'; import { FractalExplorerComponent, FractalRenderer } from '../fractal-explorer/fractal-explorer.component';
@Component({selector:'app-pythagoras-tree',standalone:true,imports:[FractalExplorerComponent],templateUrl:'./pythagoras-tree.component.html',styleUrl:'./pythagoras-tree.component.css'}) export class PythagorasTreeComponent { static title='Árbol de Pitágoras'; static route='pythagoras-tree'; readonly draw:FractalRenderer=(ctx,iterations)=>{const rec=(x:number,y:number,s:number,a:number,n:number):void=>{const dx=Math.cos(a)*s,dy=Math.sin(a)*s;ctx.strokeStyle=`hsl(${100+n*18},75%,62%)`;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+dx,y+dy);ctx.stroke();if(!n)return;rec(x+dx,y+dy,s*.72,a-.72,n-1);rec(x+dx,y+dy,s*.72,a+.72,n-1);};rec(340,600,145,-Math.PI/2,iterations+1);}; }
