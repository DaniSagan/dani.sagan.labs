import { Component } from '@angular/core'; import { FractalExplorerComponent, FractalRenderer, paint } from '../fractal-explorer/fractal-explorer.component';
@Component({selector:'app-h-tree',standalone:true,imports:[FractalExplorerComponent],templateUrl:'./h-tree.component.html',styleUrl:'./h-tree.component.css'}) export class HTreeComponent { static title='Árbol H'; static route='h-tree'; readonly draw:FractalRenderer=(ctx,iterations)=>{paint(ctx,100);const rec=(x:number,y:number,s:number,n:number):void=>{ctx.beginPath();ctx.moveTo(x-s/2,y-s/2);ctx.lineTo(x-s/2,y+s/2);ctx.moveTo(x+s/2,y-s/2);ctx.lineTo(x+s/2,y+s/2);ctx.moveTo(x-s/2,y);ctx.lineTo(x+s/2,y);ctx.stroke();if(n)[[x-s/2,y-s/2],[x-s/2,y+s/2],[x+s/2,y-s/2],[x+s/2,y+s/2]].forEach(([a,b])=>rec(a,b,s/2,n-1));};rec(340,340,390,iterations);}; }
