import { Component } from '@angular/core'; import { FractalExplorerComponent, FractalRenderer } from '../fractal-explorer/fractal-explorer.component';
@Component({selector:'app-julia-set',standalone:true,imports:[FractalExplorerComponent],templateUrl:'./julia-set.component.html',styleUrl:'./julia-set.component.css'}) export class JuliaSetComponent { static title='Conjunto de Julia'; static route='julia-set'; readonly draw:FractalRenderer=(ctx,iterations,size)=>{const image=ctx.createImageData(size,size),max=45+iterations*20;for(let py=0;py<size;py++)for(let px=0;px<size;px++){const x=(px-size/2)/(size*.27),y=(py-size/2)/(size*.27);let a=x,b=y,i=0;for(;i<max&&a*a+b*b<16;i++){[a,b]=[a*a-b*b-.745,2*a*b+.113];}const k=4*(py*size+px),v=i===max?0:Math.floor(255*i/max);image.data[k]=v;image.data[k+1]=Math.floor(v*.45);image.data[k+2]=255-v;image.data[k+3]=255;}ctx.putImageData(image,0,0);}; }
